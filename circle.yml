machine:
  environment:
    CIRCLE_CI_TEST_ENV: "TRUE"
    
test:
    override:
        - python -m tests/unit_tests

dependencies:
    override:
        - pip install -r requirements.txt
        
deployment:
  staging:
    branch: master
    heroku:
      appname: plantbot